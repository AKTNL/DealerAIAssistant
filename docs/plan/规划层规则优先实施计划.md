# 规划层规则优先实施计划

> **面向agentic工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实施此计划。步骤使用复选框（`- [ ]`）语法进行跟踪。

**目标：** 通过确定性的六场景规划层路由分析请求，然后仅让模型润色基于事实的分析结果，并配备后端强制回退和结构验证。

**架构：** 保留 `ChatService` 作为编排边界，将分析分类和报告组装移入结构化的 `AnalyticsPlan`，并保留现有的非分析聊天路径。分析请求将不再依赖开放式的工具规划；它们将使用 `RuleBasedAnalyticsService` 进行场景/范围/KPI规划，使用 `PromptFactory` 生成基于事实的提示，以及使用 `ChatService` 进行润色回复的验证和回退。

**技术栈：** Java 21、Spring Boot 3、Spring AI、JUnit 5、Mockito、Maven

---

### 任务1：引入 `AnalyticsPlan` 并重构 `RuleBasedAnalyticsService`

**文件：**
- 创建：`backend/src/main/java/com/brand/agentpoc/service/AnalyticsPlan.java`
- 创建：`backend/src/test/java/com/brand/agentpoc/service/RuleBasedAnalyticsServiceTest.java`
- 修改：`backend/src/main/java/com/brand/agentpoc/service/RuleBasedAnalyticsService.java:74-158`
- 修改：`backend/src/main/java/com/brand/agentpoc/service/RuleBasedAnalyticsService.java:717-903`

- [ ] **步骤1：编写会失败的规划层测试**

```java
package com.brand.agentpoc.service;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import com.brand.agentpoc.ai.PromptFactory;
import com.brand.agentpoc.entity.Dealer;
import com.brand.agentpoc.entity.Opportunity;
import com.brand.agentpoc.entity.Target;
import com.brand.agentpoc.repository.CampaignRepository;
import com.brand.agentpoc.repository.DealerRepository;
import com.brand.agentpoc.repository.LeadRepository;
import com.brand.agentpoc.repository.OpportunityRepository;
import com.brand.agentpoc.repository.TargetRepository;
import com.brand.agentpoc.repository.TaskRepository;
import java.time.LocalDate;
import java.util.List;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.CsvSource;

class RuleBasedAnalyticsServiceTest {

    @ParameterizedTest
    @CsvSource(
            delimiter = '|',
            textBlock = """
            Which dealers have the lowest target achievement this month?|TARGET_ACHIEVEMENT
            How does the opportunity funnel perform in Shanghai?|OPPORTUNITY_FUNNEL
            What are the current overdue sales follow-up tasks?|SALES_FOLLOW_UP
            Which campaign type has the highest conversion rate?|CAMPAIGN_PERFORMANCE
            Which dealers are outperforming others in this region?|DEALER_BENCHMARK
            What are the main lead sources and how is organic traffic trending?|LEAD_SOURCE
            """
    )
    void buildsStructuredPlanForEachScenario(String message, AnalyticsPlan.Scenario expectedScenario) {
        RuleBasedAnalyticsService service = createService();

        AnalyticsPlan plan = service.plan(message, "en");

        assertThat(plan.scenario()).isEqualTo(expectedScenario);
        assertThat(plan.scopeSummary()).isNotBlank();
        assertThat(plan.progressMessages()).hasSize(3);
        assertThat(plan.visibleThinking()).contains("1.");
        assertThat(plan.groundedReference()).contains("Scenario:");
        assertThat(plan.groundedReference()).contains("Canonical fallback report:");
        assertThat(plan.fallbackReply()).contains("## Conclusion");
        assertThat(plan.fallbackReply()).contains("## Data Support");
        assertThat(plan.fallbackReply()).contains("<table");
        assertThat(plan.fallbackReply()).contains("FOLLOW_UP_QUESTIONS:");
    }

    private RuleBasedAnalyticsService createService() {
        PromptFactory promptFactory = new PromptFactory();
        DealerRepository dealerRepository = mock(DealerRepository.class);
        OpportunityRepository opportunityRepository = mock(OpportunityRepository.class);
        CampaignRepository campaignRepository = mock(CampaignRepository.class);
        TaskRepository taskRepository = mock(TaskRepository.class);
        TargetRepository targetRepository = mock(TargetRepository.class);
        LeadRepository leadRepository = mock(LeadRepository.class);

        when(dealerRepository.findAll()).thenReturn(List.of(
                new Dealer("SH001", "Shanghai A", "Shanghai", "East Group")
        ));
        when(opportunityRepository.findAll()).thenReturn(List.of(
                new Opportunity(
                        "OP-001",
                        "SH001",
                        "Shanghai A",
                        "Shanghai",
                        "East Group",
                        "M7",
                        "Won",
                        "Website",
                        LocalDate.of(2026, 5, 1),
                        LocalDate.of(2026, 5, 31),
                        80
                )
        ));
        when(campaignRepository.findAll()).thenReturn(List.of());
        when(taskRepository.findAll()).thenReturn(List.of());
        when(targetRepository.findAll()).thenReturn(List.of(
                new Target("SH001", "Shanghai A", "Shanghai", "East Group", "M7", 2026, 5, 10, 6)
        ));
        when(leadRepository.findAll()).thenReturn(List.of());

        return new RuleBasedAnalyticsService(
                promptFactory,
                dealerRepository,
                opportunityRepository,
                campaignRepository,
                taskRepository,
                targetRepository,
                leadRepository
        );
    }
}
```

- [ ] **步骤2：运行新测试以验证它失败**

运行：`mvn -Dfrontend.skip=true test -Dtest=RuleBasedAnalyticsServiceTest`
预期：FAIL，出现诸如 `cannot find symbol: class AnalyticsPlan` 和 `cannot find symbol: method plan(java.lang.String,java.lang.String)` 的错误。

- [ ] **步骤3：添加新的分析计划模型**

```java
package com.brand.agentpoc.service;

import java.util.List;

public record AnalyticsPlan(
        Scenario scenario,
        String scopeSummary,
        List<String> progressMessages,
        String visibleThinking,
        String groundedReference,
        String fallbackReply
) {
    public enum Scenario {
        TARGET_ACHIEVEMENT,
        OPPORTUNITY_FUNNEL,
        SALES_FOLLOW_UP,
        CAMPAIGN_PERFORMANCE,
        DEALER_BENCHMARK,
        LEAD_SOURCE
    }
}
```

- [ ] **步骤4：重构 `RuleBasedAnalyticsService` 以返回结构化计划**

```java
public AnalyticsPlan plan(String message, String language) {
    AnalysisTopic topic = detectTopic(message);
    AnalysisScope scope = detectScope(message);
    String topicLabel = topic.label(language);
    String scopeSummary = scope.summary(language);

    List<String> progressMessages = buildProgressMessages(language, topicLabel);
    String visibleThinking = promptFactory.buildVisibleThinking(language, topicLabel, scopeSummary);
    String fallbackReply = switch (topic) {
        case TARGET_ACHIEVEMENT -> analyzeTargetAchievement(scope, language);
        case OPPORTUNITY_FUNNEL -> analyzeOpportunityFunnel(scope, language);
        case SALES_FOLLOW_UP -> analyzeSalesFollowUp(scope, language);
        case CAMPAIGN_PERFORMANCE -> analyzeCampaignPerformance(scope, language);
        case LEAD_SOURCE -> analyzeLeadSource(scope, language);
        case DEALER_BENCHMARK -> analyzeDealerBenchmark(scope, language);
    };

    return new AnalyticsPlan(
            topic.toScenario(),
            scopeSummary,
            progressMessages,
            visibleThinking,
            buildGroundedReference(topic, scopeSummary, fallbackReply, language),
            fallbackReply.trim()
    );
}

private String buildGroundedReference(
        AnalysisTopic topic,
        String scopeSummary,
        String fallbackReply,
        String language
) {
    return """
            Scenario: %s
            Scope: %s
            Language: %s
            Canonical fallback report:
            %s
            """.formatted(topic.name(), scopeSummary, language, fallbackReply.trim()).trim();
}
```

- [ ] **步骤5：将内部主题映射到新的公共场景**

```java
private enum AnalysisTopic {
    TARGET_ACHIEVEMENT("Target Achievement", "target achievement"),
    OPPORTUNITY_FUNNEL("Opportunity Funnel", "opportunity funnel"),
    SALES_FOLLOW_UP("Sales Follow-up", "sales follow-up"),
    CAMPAIGN_PERFORMANCE("Campaign Performance", "campaign performance"),
    LEAD_SOURCE("Lead Source", "lead source"),
    DEALER_BENCHMARK("Dealer Benchmark", "dealer benchmark");

    AnalyticsPlan.Scenario toScenario() {
        return switch (this) {
            case TARGET_ACHIEVEMENT -> AnalyticsPlan.Scenario.TARGET_ACHIEVEMENT;
            case OPPORTUNITY_FUNNEL -> AnalyticsPlan.Scenario.OPPORTUNITY_FUNNEL;
            case SALES_FOLLOW_UP -> AnalyticsPlan.Scenario.SALES_FOLLOW_UP;
            case CAMPAIGN_PERFORMANCE -> AnalyticsPlan.Scenario.CAMPAIGN_PERFORMANCE;
            case LEAD_SOURCE -> AnalyticsPlan.Scenario.LEAD_SOURCE;
            case DEALER_BENCHMARK -> AnalyticsPlan.Scenario.DEALER_BENCHMARK;
        };
    }
}
```

- [ ] **步骤6：重构分析回退回复，使 `## Data Support` 始终使用HTML `<table>`**

```java
return """
        ## Conclusion
        Within %s, **%s** has the lowest target achievement at **%s** while **%s** is the strongest dealer in the current scope.

        ## Data Support
        <table>
          <thead>
            <tr><th>Dealer</th><th>Won</th><th>Target</th><th>Achievement</th></tr>
          </thead>
          <tbody>
            <tr><td>%s</td><td>%d</td><td>%d</td><td>%s</td></tr>
            <tr><td>%s</td><td>%d</td><td>%d</td><td>%s</td></tr>
          </tbody>
        </table>

        ## Short Analysis
        - The current scope shows a clear execution gap between the weakest and strongest dealer.
        - The lowest dealer still needs a focused recovery plan on open opportunities and follow-up.

        FOLLOW_UP_QUESTIONS:
        1. Which open opportunities are most likely to close for %s?
        2. How does the task backlog look for the weakest dealer in this scope?
        """.formatted(
        scope.summary(language),
        lowest.dealerName(),
        formatPercent(lowest.achievementRate()),
        highest.dealerName(),
        lowest.dealerName(),
        lowest.wonCount(),
        lowest.targetValue(),
        formatPercent(lowest.achievementRate()),
        highest.dealerName(),
        highest.wonCount(),
        highest.targetValue(),
        formatPercent(highest.achievementRate()),
        lowest.dealerName()
);
```

- [ ] **步骤7：更新 `buildNoDataReply(...)` 以保持相同的四部分结构**

```java
return """
        ## Conclusion
        There is not enough data within %s to support a reliable %s answer.

        ## Data Support
        <table>
          <thead>
            <tr><th>Scope</th><th>Topic</th><th>Available Rows</th></tr>
          </thead>
          <tbody>
            <tr><td>%s</td><td>%s</td><td>0</td></tr>
          </tbody>
        </table>

        ## Short Analysis
        - The current filters are too narrow to support a stable analytics conclusion.
        - Widening the scope is safer than allowing the model to guess.

        FOLLOW_UP_QUESTIONS:
        1. What does this metric look like across the full sample dataset?
        2. Which nearby dealer or city scope has enough data for comparison?
        """.formatted(scope.summary(language), topicLabel, scope.summary(language), topicLabel);
```

- [ ] **步骤8：运行规划层测试以验证它通过**

运行：`mvn -Dfrontend.skip=true test -Dtest=RuleBasedAnalyticsServiceTest`
预期：PASS

- [ ] **步骤9：提交**

```bash
git add backend/src/main/java/com/brand/agentpoc/service/AnalyticsPlan.java backend/src/main/java/com/brand/agentpoc/service/RuleBasedAnalyticsService.java backend/src/test/java/com/brand/agentpoc/service/RuleBasedAnalyticsServiceTest.java
git commit -m "Refactor analytics service to build structured plans"
```

### 任务2：在 `ChatService` 中通过规划路径路由分析请求

**文件：**
- 修改：`backend/src/main/java/com/brand/agentpoc/service/ChatService.java:47-144`
- 修改：`backend/src/main/java/com/brand/agentpoc/service/ChatService.java:288-433`
- 修改：`backend/src/test/java/com/brand/agentpoc/service/ChatServiceTest.java:31-163`

- [ ] **步骤1：编写会失败的聊天服务测试（分析路由和回退）**

```java
@Test
void analyticsRequestsUseGroundedPlanningAndFallbackWhenPolishFails() {
    ChatRequest request = new ChatRequest(
            "s1",
            "Which dealers have the lowest target achievement?",
            "https://api.example.com",
            "sk-test",
            "gpt-4.1-mini"
    );
    ChatModel chatModel = mock(ChatModel.class);
    AnalyticsPlan plan = new AnalyticsPlan(
            AnalyticsPlan.Scenario.TARGET_ACHIEVEMENT,
            "Shanghai / East Group",
            List.of("Identifying the analysis theme", "Filtering target data", "Generating a structured response"),
            "1. Classify the request\n2. Compute KPIs\n3. Produce the report",
            """
            Scenario: TARGET_ACHIEVEMENT
            Scope: Shanghai / East Group
            Language: en
            Canonical fallback report:
            ## Conclusion
            fallback
            ## Data Support
            <table><tr><td>fallback</td></tr></table>
            ## Short Analysis
            fallback
            FOLLOW_UP_QUESTIONS:
            1. next?
            2. next?
            """,
            """
            ## Conclusion
            fallback
            ## Data Support
            <table><tr><td>fallback</td></tr></table>
            ## Short Analysis
            fallback
            FOLLOW_UP_QUESTIONS:
            1. next?
            2. next?
            """
    );

    when(languageDetector.detectLanguage(request.message())).thenReturn("en");
    when(modelConfigService.createChatModel(request)).thenReturn(chatModel);
    when(analyticsService.plan(request.message(), "en")).thenReturn(plan);
    when(promptFactory.buildGroundedModelPrompt("en", request.message(), "None", plan.groundedReference()))
            .thenReturn("Grounded prompt");
    when(promptFactory.buildSystemPrompt("en")).thenReturn("System prompt");
    when(chatModel.call(any(Prompt.class))).thenThrow(new RuntimeException("429 busy"));

    String reply = chatService.chat(request);

    assertThat(reply).isEqualTo(plan.fallbackReply().trim());
    verify(analyticsService).plan(request.message(), "en");
}

@Test
void analyticsRequestsRejectInvalidPolishedReplies() {
    ChatRequest request = new ChatRequest(
            "s1",
            "How does the opportunity funnel perform?",
            "https://api.example.com",
            "sk-test",
            "gpt-4.1-mini"
    );
    ChatModel chatModel = mock(ChatModel.class);
    AnalyticsPlan plan = new AnalyticsPlan(
            AnalyticsPlan.Scenario.OPPORTUNITY_FUNNEL,
            "the current sample dataset",
            List.of("Identifying the analysis theme", "Filtering funnel data", "Generating a structured response"),
            "1. Classify the request\n2. Compute KPIs\n3. Produce the report",
            "Scenario: OPPORTUNITY_FUNNEL\nScope: the current sample dataset\nLanguage: en",
            """
            ## Conclusion
            fallback
            ## Data Support
            <table><tr><td>fallback</td></tr></table>
            ## Short Analysis
            fallback
            FOLLOW_UP_QUESTIONS:
            1. next?
            2. next?
            """
    );

    when(languageDetector.detectLanguage(request.message())).thenReturn("en");
    when(modelConfigService.createChatModel(request)).thenReturn(chatModel);
    when(analyticsService.plan(request.message(), "en")).thenReturn(plan);
    when(promptFactory.buildGroundedModelPrompt("en", request.message(), "None", plan.groundedReference()))
            .thenReturn("Grounded prompt");
    when(promptFactory.buildSystemPrompt("en")).thenReturn("System prompt");
    when(chatModel.call(any(Prompt.class))).thenReturn(new ChatResponse(List.of(
            new Generation(new AssistantMessage(
                    "## Conclusion\nbad\n## Data Support\n- no html table\n## Short Analysis\nbad\nFOLLOW_UP_QUESTIONS:\n1. a\n2. b"
            ))
    )));

    String reply = chatService.chat(request);

    assertThat(reply).isEqualTo(plan.fallbackReply().trim());
}
```

- [ ] **步骤2：运行聊天服务测试以验证它们失败**

运行：`mvn -Dfrontend.skip=true test -Dtest=ChatServiceTest`
预期：FAIL，因为 `ChatService` 仍然忽略 `analyticsService` 且不验证分析输出结构。

- [ ] **步骤3：将 `ChatService.generateReply(...)` 拆分为分析路径和通用路径**

```java
private GeneratedReply generateReply(ChatRequest request, String language, boolean analyticsRequested) {
    if (analyticsRequested) {
        return generateAnalyticsReply(request, language);
    }
    return generateGeneralReply(request, language);
}

private GeneratedReply generateGeneralReply(ChatRequest request, String language) {
    ChatModel chatModel = modelConfigService.createChatModel(request);
    String reply = callConversationModel(chatModel, request.sessionId(), request.message(), language);
    return new GeneratedReply(
            ensureFollowUpQuestions(reply, language, false),
            buildModelProgressMessages(language, false),
            buildConversationThinking(language)
    );
}
```

- [ ] **步骤4：添加带有基于事实提示构造的分析规划路径**

```java
private GeneratedReply generateAnalyticsReply(ChatRequest request, String language) {
    AnalyticsPlan plan = analyticsService.plan(request.message(), language);
    ChatModel chatModel = modelConfigService.createChatModel(request);

    try {
        String polishedReply = callGroundedAnalyticsModel(
                chatModel,
                request.sessionId(),
                request.message(),
                language,
                plan
        );

        String normalizedReply = ensureFollowUpQuestions(polishedReply, language, true);
        if (!isValidAnalyticsReply(normalizedReply)) {
            normalizedReply = plan.fallbackReply().trim();
        }

        return new GeneratedReply(normalizedReply, plan.progressMessages(), plan.visibleThinking());
    } catch (Exception exception) {
        return new GeneratedReply(
                plan.fallbackReply().trim(),
                plan.progressMessages(),
                plan.visibleThinking()
        );
    }
}
```

- [ ] **步骤5：添加基于事实的模型调用和分析回复验证辅助方法**

```java
private String callGroundedAnalyticsModel(
        ChatModel chatModel,
        String sessionId,
        String message,
        String language,
        AnalyticsPlan plan
) {
    String sessionHistory = formatSessionHistory(sessionId, language);
    String userPrompt = promptFactory.buildGroundedModelPrompt(
            language,
            message,
            sessionHistory,
            plan.groundedReference()
    );

    String reply = ChatClient.create(chatModel)
            .prompt()
            .system(promptFactory.buildSystemPrompt(language))
            .user(userPrompt)
            .call()
            .content();

    if (reply == null || reply.isBlank()) {
        throw new IllegalStateException("Configured model returned an empty response.");
    }

    return reply.trim();
}

private boolean isValidAnalyticsReply(String reply) {
    String normalized = reply == null ? "" : reply.trim();
    return normalized.contains("## Conclusion")
            && normalized.contains("## Data Support")
            && normalized.contains("## Short Analysis")
            && normalized.contains("<table")
            && normalized.contains("FOLLOW_UP_QUESTIONS:");
}
```

- [ ] **步骤6：更新流式处理以发出计划特定的进度和回退回复**

```java
public void streamChat(ChatRequest request, OutputStream outputStream) throws IOException {
    validateModelSettings(request);
    String language = languageDetector.detectLanguage(request.message());
    boolean analyticsRequested = looksLikeAnalyticsRequest(request.message());
    sessionMemoryService.addUserMessage(request.sessionId(), request.message());

    try (BufferedWriter writer = new BufferedWriter(new OutputStreamWriter(outputStream, StandardCharsets.UTF_8))) {
        GeneratedReply generatedReply = generateReply(request, language, analyticsRequested);
        for (String progress : generatedReply.progressMessages()) {
            writeEvent(writer, "progress", progress);
        }
        writeEvent(writer, "message", "<think>" + generatedReply.visibleThinking() + "</think>");
        writeEvent(writer, "message", generatedReply.reply());
        writeEvent(writer, "done", "[DONE]");
        sessionMemoryService.addAssistantMessage(request.sessionId(), generatedReply.reply());
    } catch (Exception exception) {
        writeEvent(writer, "error", describeStreamFailure(exception));
    }
}
```

- [ ] **步骤7：运行聊天服务测试以验证它们通过**

运行：`mvn -Dfrontend.skip=true test -Dtest=ChatServiceTest`
预期：PASS

- [ ] **步骤8：提交**

```bash
git add backend/src/main/java/com/brand/agentpoc/service/ChatService.java backend/src/test/java/com/brand/agentpoc/service/ChatServiceTest.java
git commit -m "Route analytics chat through structured planning"
```

### 任务3：强化基于事实的提示并验证后端套件

**文件：**
- 修改：`backend/src/main/java/com/brand/agentpoc/ai/PromptFactory.java:38-76`
- 修改：`backend/src/test/java/com/brand/agentpoc/service/ChatServiceTest.java:135-163`
- 修改：`backend/src/test/java/com/brand/agentpoc/service/RuleBasedAnalyticsServiceTest.java`

- [ ] **步骤1：为所需的分析章节添加会失败的提示断言**

```java
@Test
void analyticsPromptExplicitlyRequiresStructuredSections() {
    PromptFactory promptFactory = new PromptFactory();

    String prompt = promptFactory.buildGroundedModelPrompt(
            "en",
            "Which dealers have the lowest target achievement?",
            "None",
            "Scenario: TARGET_ACHIEVEMENT"
    );

    assertThat(prompt).contains("## Conclusion");
    assertThat(prompt).contains("## Data Support");
    assertThat(prompt).contains("## Short Analysis");
    assertThat(prompt).contains("HTML <table>");
    assertThat(prompt).contains("must not be changed");
}
```

- [ ] **步骤2：运行聚焦测试以验证它们失败**

运行：`mvn -Dfrontend.skip=true test -Dtest=ChatServiceTest,RuleBasedAnalyticsServiceTest`
预期：FAIL，因为基于事实的提示尚未要求完整的四部分分析格式。

- [ ] **步骤3：强化 `PromptFactory.buildGroundedModelPrompt(...)`**

```java
return """
        Current user question:
        %s

        Recent conversation context:
        %s

        Grounded reference facts (this is the only source of truth and must not be changed):
        %s

        Rewrite the answer into a concise analytics report.
        Requirements:
        1. Answer in English.
        2. Do not change KPI values, dealer names, scope, or table facts.
        3. Do not mention internal planning, grounded references, tool names, or API names.
        4. Use exactly these sections:
           - ## Conclusion
           - ## Data Support
           - ## Short Analysis
           - FOLLOW_UP_QUESTIONS:
        5. The `## Data Support` section must include an HTML <table>, not a Markdown table.
        6. `FOLLOW_UP_QUESTIONS:` must contain exactly 2 numbered questions.
        """.formatted(userMessage, sessionHistory, groundedReference);
```

- [ ] **步骤4：重新运行聚焦测试以验证它们通过**

运行：`mvn -Dfrontend.skip=true test -Dtest=ChatServiceTest,RuleBasedAnalyticsServiceTest`
预期：PASS

- [ ] **步骤5：运行完整的后端测试套件**

运行：`mvn -Dfrontend.skip=true test`
预期：PASS，所有后端测试通过。

- [ ] **步骤6：提交**

```bash
git add backend/src/main/java/com/brand/agentpoc/ai/PromptFactory.java backend/src/test/java/com/brand/agentpoc/service/ChatServiceTest.java backend/src/test/java/com/brand/agentpoc/service/RuleBasedAnalyticsServiceTest.java
git commit -m "Harden grounded analytics prompting"
```